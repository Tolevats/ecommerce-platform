import type { Config } from "tailwindcss";

const config: Config = {
  // No 'content' needed in v4
  // No 'theme' needed here. Using @theme in CSS for customization
  plugins: [
    // Add any Tailwind plugins here in the future
    // e.g., require('@tailwindcss/forms'),
  ],
};
export default config;
